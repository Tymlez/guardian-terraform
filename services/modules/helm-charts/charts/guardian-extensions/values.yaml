# Default values for guardian-frontend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

ingress:
  enabled: true
  whitelistedIps: ""
  custom_helm_ingresses:
    enable: false
    service_name: ""
    service_port: ""
    service_path: ""

proxy:
  guardian_api_gateway: guardian-api-gateway
  guardian_frontend: guardian-frontend
