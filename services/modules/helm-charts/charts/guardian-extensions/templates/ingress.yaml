{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: guardian-ingress
    annotations:
        nginx.ingress.kubernetes.io/use-regex: "true"
        nginx.ingress.kubernetes.io/rewrite-target: /$2
        nginx.ingress.kubernetes.io/enable-rewrite-log: "true"
        nginx.ingress.kubernetes.io/proxy-connect-timeout: "180"
        nginx.ingress.kubernetes.io/proxy-read-timeout: "180"
        nginx.ingress.kubernetes.io/proxy-send-timeout: "180"
        nginx.ingress.kubernetes.io/proxy-body-size: 64mb
        # nginx.ingress.kubernetes.io/auth-type: basic
        nginx.ingress.kubernetes.io/whitelist-source-range: "{{.Values.global.guardian.whitelistedIps}}"
spec:
    ingressClassName: 'nginx'
    rules:
      - http:
          paths:
            - path: /api/v1(/|$)(.*)
              pathType: Prefix
              backend:
                service:
                  name: {{ .Values.proxy.guardian_api_gateway_host }}
                  port:
                    number: 3002
            - path: /?(/|$)(.*)
              pathType: Prefix
              backend:
                service:
                  name: {{ .Values.proxy.guardian_frontend_host}}
                  port:
                    number: 80
{{- end}}