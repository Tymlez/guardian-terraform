apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "guardian-api-docs.fullname" . }}-test-connection"
  labels:
    {{- include "guardian-api-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "guardian-api-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
